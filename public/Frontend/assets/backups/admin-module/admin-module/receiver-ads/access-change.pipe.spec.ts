import { AccessChangePipe } from './access-change.pipe';

describe('AccessChangePipe', () => {
  it('create an instance', () => {
    const pipe = new AccessChangePipe();
    expect(pipe).toBeTruthy();
  });
});
