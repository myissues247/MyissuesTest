import { StatuschangePipe } from './statuschange.pipe';

describe('StatuschangePipe', () => {
  it('create an instance', () => {
    const pipe = new StatuschangePipe();
    expect(pipe).toBeTruthy();
  });
});
